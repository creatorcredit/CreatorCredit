[build]
  # Use Vite build on Netlify: install dependencies, run the build, and publish `dist`.
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  NODE_ENV = "production"
